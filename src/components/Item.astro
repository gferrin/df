---
const { title, year, dimensions, s3Url, catagory, forSale } = Astro.props;
---

<div class="flex flex-col md:flex-row items-start mb-8 md:justify-center">     
    <img src={s3Url} alt="Art 1" class="w-full md:w-1/2 mb-4 md:mb-0">
    <div class="text-center md:text-left md:pl-4">
        <h2 class="text-lg font-semibold italic">{{ title }}, {{ year }}</h2>
        <p class="mb-1">{{ catagory }}</p> 
        <p class="mb-1">{{ dimensions }}</p>
        <a href="#" class="text-blue-500 hover:underline">Inquire to Purchase</a>
        <!-- <div class="flex justify-center md:justify-start space-x-4 mt-4">
            <a href="#" class="text-gray-500 hover:text-gray-700"><i class="fa fa-facebook fa-2x"></i></a>
            <a href="#" class="text-gray-500 hover:text-gray-700"><i class="fa fa-twitter fa-2x"></i></a>
            <a href="#" class="text-gray-500 hover:text-gray-700"><i class="fa fa-pinterest fa-2x"></i></a>
        </div> -->   
    </div>
</div>
